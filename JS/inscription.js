let onClickAction = document.querySelectorAll('button');
let reset = document.getElementById('reset');

onClickActionButton => {
    reset.addEventListener('click', (e)=> alert("It's Done"))
}
onClickActionButton(); 